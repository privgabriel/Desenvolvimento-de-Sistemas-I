// Declare um objeto pessoa com as propriedades nome e idade. 
// Em seguida, use a desestruturação para extrair essas propriedades e atribuí-las a variáveis separadas chamadas nome e idade.
const pessoa = { nome: "Gabriel", idade: 22}
const { nome, idade } = pessoa;