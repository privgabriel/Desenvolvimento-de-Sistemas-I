// Crie uma função chamada somarNumeros que aceita um array de números e retorna a soma de todos os números.

function SomarNumeros(numeros7:number[]): number[] {
    return numeros.filter((total, numero) => total + numero, 0)
}

const numeros7: number[] = [1, 2, 3, 4, 5, 6, 7, 8, 10];
const dobroNumeros = SomarNumeros(numeros7)
console.log(dobroNumeros)